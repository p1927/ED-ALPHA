# Demo Walkthrough

Run the timed demo walkthrough locally:

```bash
corepack enable
pnpm install
pnpm dev
```

Open:

```text
http://localhost:3000/walkthrough
```

The page shows the ED-ALPHA dashboard with narration, subtitles, playback
controls, and timed highlights.
